---
"@dethink/components": minor
---

Add an opt-in `spring` prop to Switch. The thumb uses a lazily loaded Motion spring, supports all sizes and RTL, and respects reduced motion while preserving native input behavior.
