---
"@dethink/components": minor
---

Add an optional shell-spanning bottom bar with `SidebarShellFooter span="shell"`. Preserve content-width footers by default and correctly size layouts without header or footer. Make physical sidebar edges consistent in RTL mobile drawers, preserve inherited drawer direction, support fragment-wrapped shell regions, and align right-side shell DOM order with placement. Refresh sidebar toggles with navigation strokes and directional chevrons.
