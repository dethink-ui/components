---
"@dethink/components": minor
---

Add the data-driven Steps component with conditional branch updates,
horizontal and vertical layouts, optional navigation, progress, status
overrides, typed custom rendering, and reduced-motion-aware Motion choreography.
