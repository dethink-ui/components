---
"@dethink/components": minor
---

Add DotMatrixBackground, a landing-page background that rolls soft brightness pulses through a token-colored dot grid at seeded origins. Pulses are masked copies of the same pattern so dots brighten in place. SSR-deterministic via seed, offscreen-paused, with a designed reduced-motion static fallback and registry metadata.
