---
"@dethink/components": minor
---

Add GridBeamsBackground, the first landing-page background component: an animated token-colored line grid with seeded light beams tracing along grid lines. Includes the shared seeded-random utility for SSR-deterministic decorative layouts, a reduced-motion static fallback, offscreen pause, and registry metadata.
