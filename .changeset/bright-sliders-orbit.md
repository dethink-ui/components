---
"@dethink/components": minor
---

Add accessible horizontal Slider controls for numeric values, ranges, and labelled
stepper stops. Include an inset XL size, typed callbacks and form values, tokenized
styling slots, and the optional Motion-based ExpressiveSlider companion.
