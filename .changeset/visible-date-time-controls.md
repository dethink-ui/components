---
"@dethink/components": patch
---

Show the calendar and accessible time controls together in DateTimePicker by default, with a clock shortcut, editable time segments, quick picks, and a Done action. Disable unavailable quick picks and presets. Set `timeSelector={false}` to retain the calendar-only popover. Values still update immediately; Done closes the popover.
