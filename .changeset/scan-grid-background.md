---
"@dethink/components": minor
---

Add ScanGridBackground, a landing-page background that sweeps a scanning highlight band across a token-colored line grid, vertically or horizontally. SSR-deterministic via seed, offscreen-paused, with a designed reduced-motion static fallback and registry metadata.
