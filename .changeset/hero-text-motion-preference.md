---
"@dethink/components": patch
---

Read the browser's reduced-motion preference after hydration and respond to preference changes in HeroTextAnimation, preserving explicit provider overrides.

Protect descenders in masked line reveals, retain the blurred starting state for in-view focus reveals, and start scroll-responsive movement while the heading is visible using a stable, untransformed scroll anchor.

Spread scroll-responsive motion across the visible viewport and gently scale the heading back so the response remains perceptible during normal scrolling.
