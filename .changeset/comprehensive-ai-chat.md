---
"@dethink/components": minor
---

Add a composable, provider-independent AI chat family: Chat, PromptInput, ChatMessage, MessageContent, MessageActions, ChatActivity, TypingIndicator, ToolCallCard, CitationCard, AttachmentBubble, MessageList, MessageScroller, ConversationList, and ModelPicker.

Include controlled lifecycle events, approval callbacks, streaming and recovery states, file/model-aware composition, anchored scrolling, optional measured windowing, and safe Markdown through the optional `@dethink/components/chat-markdown` entry point. Add focused registry items, accessible examples, and the AI Chat Studio recipe.
