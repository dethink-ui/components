---
"@dethink/components": minor
---

Add LightStreaksBackground, a landing-page background that sweeps blurred diagonal light streaks across hero surfaces on staggered seeded loops. SSR-deterministic via seed, offscreen-paused, with a designed reduced-motion static fallback and registry metadata.
