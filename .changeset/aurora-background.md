---
"@dethink/components": minor
---

Add AuroraBackground, a landing-page background of flowing aurora ribbons — heavily blurred gradient bands that drift, breathe, and pulse on slow mirrored loops. Ribbon hues derive from the active tone token via CSS relative color syntax with a designed mono-hue fallback. SSR-deterministic via seed, offscreen-paused, with a designed reduced-motion static fallback and registry metadata.
