---
"@dethink/components": patch
---

Fix AsyncSelect controlled and late-arriving labels, preserve server-filtered result windows, and keep loading/error feedback accessible inside open popups. Retry supports keyboard focus, and empty guidance no longer competes with field validation. Add Combobox popup feedback composition and a MultiSelect filtering opt-out used by AsyncSelect.
