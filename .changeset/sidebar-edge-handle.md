---
"@dethink/components": patch
---

Move the default Sidebar rail control to a compact edge-mounted handle with a
Motion-powered direction-aware chevron, while preserving inline trigger support
and reduced-motion behavior.
