---
"@dethink/components": patch
---

Keep bounded pagination windows the same size near the first and last pages so controls do not jump when paging. Fix the showcase's URL-backed example to reflect the query string and navigate without document reloads or scroll resets.
